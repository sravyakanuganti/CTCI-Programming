import java.util.HashSet;
import java.util.Set;

public class IsUnique {
	
	public boolean AreUniqueChars(String s)
	{
		if(s.length() > 128)
			return false;
		
		Set<Character> uniqueChras = new HashSet<>();
		
		for(int i = 0; i < s.length(); i++)
		{
			if(uniqueChars.contains(s.charAt(index)))
		}
		
		return false;
		
	}

}
